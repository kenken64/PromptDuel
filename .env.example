# ========================================
# Prompt Duel - Environment Variables
# ========================================
# Copy this file to .env for local development

# ----------------------------------------
# AI Provider Authentication
# ----------------------------------------
# Anthropic API key for Claude Code CLI authentication (required for headless/Docker)
# When set, bypasses interactive browser login. Uses API pay-as-you-go billing.
ANTHROPIC_API_KEY=sk-ant-api03-xxxxx

# Google Gemini API key (for multi-provider support)
# Get your key at: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=AIzaSyxxxxx

# OpenAI API key (for multi-provider support)
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-xxxxx

# ----------------------------------------
# Email Configuration (Resend)
# ----------------------------------------
# Resend API key for password reset emails
# Get your key at: https://resend.com/api-keys
RESEND_API_KEY=re_xxxxx

# Sender email address (must be verified in Resend)
# Default: onboarding@resend.dev (Resend test sender)
SENDER_EMAIL=onboarding@resend.dev

# Frontend URL for password reset links
FRONTEND_URL=http://localhost:5173

# ----------------------------------------
# Backend Configuration
# ----------------------------------------
# SQLite database file path
# Default: sqlite.db (relative to backend/)
# For Docker/Railway: /app/data/sqlite.db
DATABASE_URL=sqlite.db

# ----------------------------------------
# Claude Code Server Configuration
# ----------------------------------------
# Workspace directory for player files (absolute or relative path)
# Default: ./workspaces (relative to project root)
# For Docker/Railway: /app/workspaces
WORKSPACES_DIR=./workspaces

# ----------------------------------------
# Frontend Configuration (Vite)
# ----------------------------------------
# Backend API URL (Elysia server)
# Default: http://localhost:3000
VITE_API_URL=http://localhost:3000

# Claude Code WebSocket server URL
# Default: ws://localhost:3001
VITE_WS_URL=ws://localhost:3001

# ----------------------------------------
# Railway Deployment Notes
# ----------------------------------------
# For Railway deployment, you'll need to:
# 1. Set ANTHROPIC_API_KEY in Railway dashboard
# 2. Update VITE_API_URL to your backend Railway URL (https://...)
# 3. Update VITE_WS_URL to your claude-code-server Railway URL (wss://...)
# 4. Set DATABASE_URL to /app/data/sqlite.db (with Railway volume)
# 5. Set WORKSPACES_DIR to /app/workspaces
